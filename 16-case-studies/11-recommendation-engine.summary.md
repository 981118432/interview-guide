# 案例研究：AI 推荐引擎：中文概要

本篇以 5000 万用户的流媒体推荐为例，采用离线用户/内容嵌入、ANN 候选召回、Cross-Encoder 重排序和 LLM 解释的分层架构。LLM 只生成最终推荐理由，不承担大规模排序；内容对级解释缓存可使 p95 延迟满足 200ms 目标。

面试重点是规模成本、解释事实性、冷启动分层方案、按新近度更新用户嵌入，以及用 A/B 测试观察点击率、观看时长和跳过率。
