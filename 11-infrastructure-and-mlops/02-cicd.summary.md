# LLM 应用的 CI/CD：中文概要

LLM 的 CI/CD 与传统软件不同：输出具有概率性，Prompt、模型、数据和 RAG 索引都可能变化，评测也更慢、更贵。建议建立静态校验、单元测试、黄金集、LLM 质量评测和质量门禁组成的流水线。

发布采用 shadow 与金丝雀策略，按错误率、P99 延迟、质量分和用户负反馈配置自动回滚。核心原则是把 Prompt 与模型当作可版本化的软件资产，以统计阈值控制质量，而不是用一次确定性输出判断系统是否正确。
