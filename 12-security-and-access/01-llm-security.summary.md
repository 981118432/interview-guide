# LLM 安全：中文概要

本篇覆盖 Prompt 注入、越狱、数据提取、间接注入、模型投毒、数据泄露、输出安全、多租户隔离和工具权限。生产系统应采用纵深防御：输入清理与注入检测、权限感知检索、System Prompt 保护、输出与 PII 校验、沙箱执行、参数化操作、限流和审计日志。

面试中要强调：LLM 输出不可信，不能直接执行代码、SQL 或 HTML；RAG 必须在数据库层按租户过滤，不能先检索全部内容再过滤；安全测试应包含注入测试、红队测试和持续监控。间接注入防御还要传递内容信任级别，并依据不可信内容关闭写操作工具。
